
//add function using keyword export
export function add(x, y) {
    return x + y;
  }
  
  //subtract
  export function sub(x, y) {
    return x - y;
  }
  
  //greeting
  export default function greeting(name) {
    return "Hi " + name;
  }